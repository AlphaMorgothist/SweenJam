using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class GreenPotion : Potion
{
    // Start is called before the first frame update
    void Start()
    {
        potionType = TrapTypes.SLOW;
    }
}
